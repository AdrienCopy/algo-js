(() => {
    // YOUR CODE HERE
    function rand10() {
        let number = Math.floor(Math.random() * 10) + 1; // math floor arrondi, math floor donne un nombre entre 0 et 0,999... mutlipier par 10 = entre 0 et 9. ensuite + 1 permet d'avoir entre 1 et 10.
        console.log(number);
    }

    rand()
})();

